{% if property is defined %}
    {{ grid(grid, gridTemplate|default('@APYDataGrid/grid.html.twig'), '', {'property': property, 'idRendered': idRendered}) }}
{% else %}
    {{ grid(grid, '@APYDataGrid/grid.html.twig') }}
{% endif %}
